<% if color.errors.any? %>
  <%= render 'layouts/errors', errors: color.errors %>
<% end %>

<div id="<%= dom_id color %>">
  <p>
    <strong>Cod:</strong>
    <%= color.cod %>
  </p>

  <p>
    <strong>Name:</strong>
    <%= color.name %>
  </p>

</div>
